# Keep the shared edge Nginx from serving more than one APK at a time.
# The key is the server name so this is one global slot, not one slot per client.
limit_conn_zone $server_name zone=calorieai_update_downloads:64k;
