# Keep the shared edge bounded while allowing two downloads overall and one per client.
limit_conn_zone $server_name zone=calorieai_update_downloads:64k;
limit_conn_zone $binary_remote_addr zone=calorieai_update_download_clients:1m;
